---
title: "Send Invitation"
api: "POST /organizations/{organizationId}/invitations"
description: "Send an invitation."
---

<Info>Requires `invitations:write` permission.</Info>

## Headers

<ParamField header="Authorization" type="string" required>
  Bearer token.
</ParamField>

## Path Parameters

<ParamField path="organizationId" type="string" required>Organization UUID.</ParamField>

## Request Body

<ParamField body="email" type="string" required>Email to invite.</ParamField>
<ParamField body="expiresInDays" type="integer">Number of days until expiration.</ParamField>
<ParamField body="inviterUserId" type="string">Inviter user ID.</ParamField>
<ParamField body="roleSlug" type="string">Role to assign on acceptance.</ParamField>

## Response

<ResponseField name="invitationId" type="string">Invitation ID.</ResponseField>
<ResponseField name="organizationId" type="string">Organization UUID.</ResponseField>
<ResponseField name="inviterUserId" type="string">Inviter user ID.</ResponseField>
<ResponseField name="acceptedUserId" type="string">Accepted user ID.</ResponseField>
<ResponseField name="email" type="string">Invited email.</ResponseField>
<ResponseField name="token" type="string">Invitation token.</ResponseField>
<ResponseField name="acceptInvitationUrl" type="string">URL to accept the invitation.</ResponseField>
<ResponseField name="roleSlug" type="string">Role slug.</ResponseField>
<ResponseField name="state" type="string">Invitation state.</ResponseField>
<ResponseField name="expiresAt" type="string">Expiration date.</ResponseField>
<ResponseField name="acceptedAt" type="string">Acceptance timestamp.</ResponseField>
<ResponseField name="revokedAt" type="string">Revocation timestamp.</ResponseField>
<ResponseField name="createdAt" type="string">Creation timestamp.</ResponseField>
<ResponseField name="updatedAt" type="string">Last update timestamp.</ResponseField>

<ResponseExample>
```json 201
{
  "invitationId": "inv_abc123",
  "organizationId": "019525fd-4c38-7e30-a5c1-b6e3f4d8a9c2",
  "inviterUserId": "019525fd-56a8-7db4-8c3e-2a1b4d6f8e0c",
  "acceptedUserId": null,
  "email": "jane@example.com",
  "token": "tok_abc123",
  "acceptInvitationUrl": "https://app.awsales.com/invite?token=tok_abc123",
  "roleSlug": "member",
  "state": "PENDING",
  "expiresAt": "2026-04-25T14:00:00.000Z",
  "acceptedAt": null,
  "revokedAt": null,
  "createdAt": "2026-03-25T14:00:00.000Z",
  "updatedAt": "2026-03-25T14:00:00.000Z"
}
```
```json 400
{
  "statusCode": 400,
  "message": "Bad Request Error",
  "errors": [{"message": "required", "path": "field"}]
}
```
```json 401
{
  "statusCode": 401,
  "message": "Unauthorized"
}
```
```json 403
{
  "statusCode": 403,
  "message": "Forbidden"
}
```
```json 404
{
  "statusCode": 404,
  "message": "organization.not_found",
  "resourceId": "019525fd-4c38-7e30-a5c1-b6e3f4d8a9c2"
}
```
```json 409
{
  "statusCode": 409,
  "message": "invitation.already_invited"
}
```
```json 500
{
  "statusCode": 500,
  "message": "Internal Server Error"
}
```
</ResponseExample>

## Error Codes

| Status | Code | Description |
|--------|------|-------------|
| `400` | `validation_error` | Invalid input or missing required fields |
| `401` | `unauthorized` | Missing or invalid authentication |
| `403` | `forbidden` | Insufficient permissions |
| `404` | `organization.not_found` | Organization not found |
| `409` | `invitation.already_invited` | User has already been invited |
| `500` | `internal_server_error` | An unexpected error occurred |
