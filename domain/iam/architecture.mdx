---
title: "Architecture"
description: "IAM system architecture and design decisions."
---

## System Components

```mermaid
graph TD
    A[Client] -->|JWT Bearer Token| B[API Gateway]
    B --> C[IAM Module]
    C --> D[WorkOS]
    C --> E[PostgreSQL]
    C --> F[Redis Cache]
```

## Design Decisions

### JWT Authentication
JWT access tokens contain user identity and organization context, validated statelessly on every request via signature verification.

### Cache Strategy
Users, organizations, and roles cached in Redis by ID. Invalidated on writes.
